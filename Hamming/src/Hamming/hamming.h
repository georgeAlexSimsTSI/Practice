#if !defined(HAMMING_H)
#define HAMMING_H
#include <stdexcept>
#include <string>
using std::string;

namespace hamming
{
    int compute(string x, string y);
} // namespace hamming

#endif // HAMMING_H